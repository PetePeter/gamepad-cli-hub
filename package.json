{
  "name": "gamepad-cli-hub",
  "version": "0.1.0",
  "main": "dist-electron/main.js",
  "type": "module",
  "description": "Xbox controller to multi-CLI session manager with GUI",
  "scripts": {
    "start": "npm run build && electron .",
    "start:cli": "tsx src/index.ts",
    "dev": "npm run build && electron .",
    "dev:watch": "tsx watch src/index.ts",
    "build": "npm run build:electron && npm run build:preload && npm run build:renderer",
    "build:electron": "esbuild src/electron/main.ts --bundle --platform=node --external:electron --external:@jitsi/robotjs --external:yaml --external:systeminformation --external:winston --external:winston-daily-rotate-file --outfile=dist-electron/main.js --format=esm --sourcemap",
    "build:preload": "esbuild src/electron/preload.ts --bundle --platform=node --external:electron --outfile=dist-electron/preload.cjs --format=cjs --sourcemap",
    "build:renderer": "esbuild renderer/main.ts --bundle --sourcemap --outfile=renderer/main.js --format=esm --target=es2022",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "keywords": [
    "gamepad",
    "cli",
    "controller",
    "terminal"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@jitsi/robotjs": "^0.6.21",
    "@types/yaml": "^1.9.6",
    "systeminformation": "^5.31.5",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/winston": "^2.4.4",
    "@vitest/ui": "^4.1.0",
    "electron": "^41.0.3",
    "esbuild": "^0.27.4",
    "tsx": "^4.21.0",
    "vitest": "^4.1.0"
  }
}
